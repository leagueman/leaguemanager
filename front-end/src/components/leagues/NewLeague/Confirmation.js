import React from 'react';

const Confirmation = (props) => {
    console.log(props.divisionsObject)
    return (
        <div>
            Show all the divisions neatly here....
        </div>
    );
};

export default Confirmation;