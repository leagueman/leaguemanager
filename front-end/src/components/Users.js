import React, {Fragment} from 'react';

const Users = (props) => {
     let userList = 3 //props.users.map((user)=>{
    //     user.title
    // })
    return (
        <Fragment>
            {userList}
        </Fragment>
    );
};

export default Users;