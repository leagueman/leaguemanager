import React from 'react';
import RegisterContainer from './RegisterContainer'
const Register = () => {
    return (
        <div>
            <RegisterContainer />
        </div>
    );
};

export default Register;